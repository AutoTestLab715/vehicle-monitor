# ESP32 车载监控系统 — 硬件连接说明

本文档说明本项目的传感器、执行器、按键与语音模块如何连接到 **ESP32 开发板**，并与固件 `esp32-firmware/config.h` 中的引脚定义一致。

> 固件路径：`esp32-firmware/VehicleMonitor/VehicleMonitor.ino`  
> 引脚配置：`esp32-firmware/config.h`（改线后需同步修改并重新烧录）

---

## 1. 系统框图

```
                    ┌─────────────────────────────────────────┐
                    │              ESP32 主控                  │
                    │                                         │
   HC-SR04 ────────►│ GPIO5(TRIG)  GPIO18(ECHO)   超声波      │
   DHT22  ────────►│ GPIO4                         温湿度      │
   MQ-2   ────────►│ GPIO34(ADC)                   烟雾模拟量  │
   按键×2 ────────►│ GPIO0 / GPIO35                本地控制    │
   语音模块 ──────►│ GPIO16(RX) GPIO17(TX) UART2   语音指令    │
                    │                                         │
   继电器(风扇) ◄──│ GPIO23                        执行器      │
   蜂鸣器     ◄──│ GPIO22                                    │
   报警 LED   ◄──│ GPIO21                                    │
   舵机(车窗) ◄──│ GPIO19                        SG90        │
                    │                                         │
                    │ USB 5V / 外接 5V 供电 ──► GND 共地        │
                    └─────────────────────────────────────────┘
                              │ WiFi
                              ▼
                     Flask 服务器 + MQTT + Web/手机
```

---

## 2. 物料清单（BOM）

| 序号 | 名称 | 型号/规格 | 数量 | 用途 |
|------|------|-----------|------|------|
| 1 | 主控板 | ESP32-DevKitC / ESP32-WROOM-32 | 1 | 采集、控制、联网 |
| 2 | 超声波模块 | HC-SR04 | 1 | 测距（碰撞预警） |
| 3 | 温湿度传感器 | DHT22（AM2302） | 1 | 车内温湿度 |
| 4 | 烟雾传感器 | MQ-2（带 AO 模拟输出） | 1 | 烟雾浓度 |
| 5 | 继电器模块 | 5V 单路，**低电平或高电平触发**（见下文） | 1 | 控制风扇/负载 |
| 6 | 蜂鸣器 | 有源蜂鸣器 5V | 1 | 声光报警 |
| 7 | LED | 5mm LED + 220Ω～1kΩ 限流电阻 | 1 | 报警指示灯 |
| 8 | 舵机 | SG90（或兼容 9g 舵机） | 1 | 模拟车窗开闭 |
| 9 | 轻触按键 | 6×6 等，常开 | 2 | 风扇/警报本地切换 |
| 10 | 语音模块 | 串口语音识别模块（9600 波特率，输出文本） | 1 | 可选，语音控制 |
| 11 | 杜邦线 / 面包板 | — | 若干 | 接线 |
| 12 | 电源 | 5V ≥2A（舵机+传感器+ESP32） | 1 | 建议外接，不要只靠 USB |

**可选辅材**

- HC-SR04 Echo 分压电阻：1kΩ + 2kΩ（Echo 5V → ESP32 3.3V）
- DHT22 数据线上拉：4.7kΩ～10kΩ 到 3.3V
- 舵机独立 5V 供电模块（与 ESP32 共 GND）

---

## 3. ESP32 引脚总表

与 `config.h` 默认值一致：

| 功能 | 宏定义 | GPIO | 方向 | 说明 |
|------|--------|------|------|------|
| 超声波 Trig | `PIN_ULTRASONIC_TRIG` | **5** | 输出 | 触发测距 |
| 超声波 Echo | `PIN_ULTRASONIC_ECHO` | **18** | 输入 | 回波，建议分压 |
| 温湿度 DHT22 | `PIN_DHT` | **4** | 双向 | 单总线数据 |
| 烟雾 MQ-2 AO | `PIN_SMOKE` | **34** | 输入(ADC) | 仅输入脚，模拟读数 |
| 风扇按键 | `PIN_BTN_FAN` | **0** | 输入 | 内部上拉，按下接 GND |
| 警报按键 | `PIN_BTN_ALARM` | **35** | 输入 | 内部上拉，按下接 GND |
| 语音 RX | `PIN_VOICE_RX` | **16** | 输入 | 接语音模块 TX |
| 语音 TX | `PIN_VOICE_TX` | **17** | 输出 | 接语音模块 RX |
| 风扇继电器 | `PIN_FAN_RELAY` | **23** | 输出 | 高电平=吸合（见说明） |
| 蜂鸣器 | `PIN_BUZZER` | **22** | 输出 | 高电平=响 |
| 报警 LED | `PIN_ALARM_LED` | **21** | 输出 | 高电平=亮 |
| 车窗舵机信号 | `PIN_WINDOW_SERVO` | **19** | 输出(PWM) | 舵机橙/黄线 |

**电源**

| ESP32 引脚 | 接法 |
|------------|------|
| 3.3V | DHT22 VCC、语音模块 3.3V/5V（视模块而定） |
| 5V (VIN/VUSB) | HC-SR04 VCC、MQ-2 VCC、继电器 VCC、蜂鸣器、舵机电源（大电流建议外接 5V） |
| GND | 所有模块 GND 必须共地 |

---

## 4. 各模块详细接线

### 4.1 HC-SR04 超声波

| HC-SR04 | ESP32 |
|---------|-------|
| VCC | 5V |
| GND | GND |
| Trig | GPIO **5** |
| Echo | GPIO **18**（经分压后接入，见下） |

**Echo 电平匹配（重要）**  
HC-SR04 Echo 输出为 **5V**，ESP32 GPIO 建议按 **3.3V** 处理。推荐分压：

```
Echo ── 1kΩ ──► GPIO18
                  │
                 2kΩ
                  │
                 GND
```

> 若无分压，长期 5V 接入可能损坏 ESP32，请务必处理。

---

### 4.2 DHT22 温湿度

| DHT22 | ESP32 |
|-------|-------|
| VCC | 3.3V |
| GND | GND |
| DATA | GPIO **4** |

DATA 与 VCC 之间加 **4.7kΩ～10kΩ 上拉电阻**。  
传感器与 ESP32 连线尽量短（< 20cm），减少干扰。

---

### 4.3 MQ-2 烟雾传感器

| MQ-2 | ESP32 / 电源 |
|------|----------------|
| VCC | 5V |
| GND | GND |
| AO（模拟输出） | GPIO **34** |
| DO（数字输出） | **不接**（固件使用 AO 模拟量） |

**使用注意**

1. 上电后需 **预热 1～2 分钟**，读数才较稳定。
2. 灵敏度可通过模块上的电位器调节。
3. GPIO34 属于 **ADC1**，WiFi 开启时仍可采样。
4. 阈值在 `config.h`：`SMOKE_WARN_RAW=2000`，`SMOKE_DANGER_RAW=2800`，需根据实际环境标定。

---

### 4.4 本地按键（2 个）

固件使用 **内部上拉** `INPUT_PULLUP`：**按下 = LOW**。

每个按键接法相同：

```
GPIO0 或 GPIO35 ── 按键 ── GND
```

| 按键 | GPIO | 功能 |
|------|------|------|
| 风扇键 | **0** | 按一下切换风扇开/关 |
| 警报键 | **35** | 按一下切换声光警报开/关 |

**注意**

- **GPIO0** 也是 ESP32 的 **BOOT 脚**：烧录或复位时请勿按住该键，否则可能无法下载程序。
- **GPIO35** 只能输入，不能输出，适合做按键。
- 固件防抖 **300ms**；手动关闭后约 **1 秒**内安全机制不强制，之后若仍危险会再次联动。

> 本项目**没有**车窗物理按键，车窗仅能通过 Web/手机/语音/MQTT 控制。

---

### 4.5 继电器 + 风扇（或负载）

| 继电器模块 | ESP32 / 负载 |
|------------|----------------|
| VCC | 5V |
| GND | GND |
| IN / SIG | GPIO **23** |
| COM / NO | 串联接入风扇电源回路 |

固件逻辑：**GPIO23 = HIGH → 风扇开**，**LOW → 关**。

```
        +5V/12V 风扇电源+
              │
           [继电器 COM─NO]
              │
           风扇 +
              │
           风扇 - ──► 电源 GND
```

**若你的继电器是「低电平触发」**（IN 接 GND 才吸合），有两种做法：

1. 改硬件：部分模块背面有跳线可切换高/低触发；或  
2. 改固件：在 `applyActuators()` 里对 `PIN_FAN_RELAY` 取反。

**安全**

- 大电流风扇不要从 ESP32 板载 5V 直接取电，应使用 **独立 5V/12V 电源**，并与 ESP32 **共 GND**。
- 继电器控制 **低压侧**；若接 mains 220V 需由专业人员操作，本实训默认 **5V/12V 演示负载**。

---

### 4.6 蜂鸣器 + 报警 LED

**有源蜂鸣器（5V）**

| 蜂鸣器 | 接法 |
|--------|------|
| + | 5V（或经三极管/MOS 由 GPIO22 驱动） |
| - | GPIO **22**（简单演示可串限流；推荐 NPN 三极管驱动） |

**推荐驱动（三极管）**

```
GPIO22 ── 1kΩ ── NPN 基极
                NPN 发射极 ── GND
                NPN 集电极 ── 蜂鸣器 - 
蜂鸣器 + ── 5V
```

**LED**

```
GPIO21 ── 220Ω~1kΩ ── LED+ ── LED- ── GND
```

固件：**GPIO22 / GPIO21 = HIGH** 时蜂鸣器响、LED 亮（与 `actuators.alarm` 同步）。

---

### 4.7 SG90 舵机（车窗）

| 舵机线色（常见） | 接法 |
|------------------|------|
| 棕/黑 GND | GND（与 ESP32 共地） |
| 红 VCC | **外接 5V**（≥1A，不要只从 ESP32 5V 取电） |
| 橙/黄 信号 | GPIO **19** |

固件角度（`config.h`）：

- 关窗：`WINDOW_CLOSED_ANGLE = 0`
- 开窗：`WINDOW_OPEN_ANGLE = 90`

---

### 4.8 语音模块（UART，可选）

使用 ESP32 **UART2**，波特率 **9600**（`VOICE_BAUD`）。

| 语音模块 | ESP32 |
|----------|-------|
| TX | GPIO **16**（ESP32 RX） |
| RX | GPIO **17**（ESP32 TX） |
| VCC | 3.3V 或 5V（按模块手册） |
| GND | GND |

**交叉连接**：模块 TX → ESP32 RX(16)，模块 RX → ESP32 TX(17)。

固件从串口读取 **一行文本**（以 `\n` 或 `\r` 结束），识别关键词例如：

| 语音文本（示例） | 动作 |
|------------------|------|
| 开风扇 / 打开风扇 | 开风扇 |
| 关风扇 / 关闭风扇 | 关风扇 |
| 开警报 / 打开警报 | 开警报 |
| 关警报 / 关闭警报 | 关警报 |
| 开窗 / 打开车窗 | 开窗 |
| 关窗 / 关闭车窗 | 关窗 |

具体词条以 `VehicleMonitor.ino` 中 `handleVoiceCommand()` 为准；不同语音模块需在模块端配置相同词条，并通过串口输出 **UTF-8 中文文本**。

---

## 5. 推荐接线顺序

1. **先共地**：ESP32 GND 与所有模块 GND 连在一起。  
2. **传感器**：HC-SR04、DHT22、MQ-2 接到对应 GPIO。  
3. **执行器**：继电器、蜂鸣器、LED、舵机（舵机用大电流 5V）。  
4. **按键**：GPIO0、GPIO35 各接一键到 GND。  
5. **语音模块**（可选）：UART 交叉接好。  
6. **上电前检查**：无短路、无 5V 直入 3.3V GPIO、舵机/风扇不由 ESP32 弱 5V 硬带。

---

## 6. 供电建议

| 场景 | 建议 |
|------|------|
| 仅 ESP32 + 传感器 | USB 5V 1A 可勉强演示 |
| 含舵机 + 继电器 + 蜂鸣器 | **外接 5V 2A** 电源，ESP32 与舵机 **共 GND** |
| 车载演示 | 5V 降压模块（如 USB 车载头）+ 独立 5V 给舵机 |

---

## 7. 烧录与网络配置

1. Arduino IDE 选择板型：**ESP32 Dev Module**。  
2. 打开 `esp32-firmware/VehicleMonitor/VehicleMonitor.ino`。  
3. 编辑 `config.h`：

```cpp
#define WIFI_SSID       "你的WiFi"
#define WIFI_PASSWORD   "你的WiFi密码"
#define SERVER_HOST     "192.168.1.100"   // 电脑 Flask 的局域网 IP
#define MQTT_BROKER     "192.168.1.100"   // Mosquitto 所在 IP
#define DEVICE_ID       "vehicle-001"
```

4. 安装库：**DHT sensor library**、**ArduinoJson**、**ESP32Servo**、**PubSubClient**。  
5. 烧录时 **不要按住 GPIO0**。  
6. 串口监视器 115200 查看 WiFi / MQTT 连接日志。

---

## 8. 上电自检

| 步骤 | 预期现象 |
|------|----------|
| 上电 | ESP32 正常启动，串口有 `Vehicle Monitor ESP32` |
| WiFi | 串口显示 `WiFi OK, IP: x.x.x.x` |
| MQTT | 串口显示 `MQTT OK` |
| 温湿度 | Web 页温度/湿度有数值（非 `--`） |
| 超声波 | 手靠近探头，距离值变小 |
| MQ-2 | 预热后 `smokeRaw` 有变化 |
| 按风扇键 | 继电器吸合/风扇转，Web 上 fan 变开 |
| 按警报键 | 蜂鸣器响、LED 亮 |
| Web 控制 | http://服务器:3000 可远程开关 |

---

## 9. 常见问题

**Q: 超声波距离一直 999 或不变？**  
检查 Trig/Echo 是否接反；Echo 是否做了 5V→3.3V 分压；HC-SR04 是否 5V 供电。

**Q: DHT22 读数 NaN？**  
检查 DATA 是否 GPIO4、是否有上拉、线是否过长。

**Q: 继电器不动作？**  
确认 IN 接 GPIO23；确认模块是高电平还是低电平触发，必要时改跳线或改固件取反。

**Q: 舵机抖动或不转？**  
必须使用 **独立 5V 大电流** 供电，并与 ESP32 共 GND；信号线接 GPIO19。

**Q: 语音无反应？**  
确认 TX/RX 交叉、波特率 9600；模块是否输出带换行符的文本；串口监视器应能看到 `Voice: xxx`。

**Q: GPIO0 烧录失败？**  
烧录时勿按 GPIO0 键；或按住 BOOT 再点 EN 进入下载模式。

---

## 10. 引脚修改

若你的开发板引脚冲突，只需修改 `esp32-firmware/config.h` 中对应 `#define`，重新编译烧录即可。

**选用 GPIO 时注意**

- GPIO **34～39** 仅输入，可作 ADC/按键，不能作输出。  
- GPIO **6～11** 通常连接 Flash，不要使用。  
- GPIO **0 / 2 / 15** 与启动模式相关，谨慎使用。

---

## 11. 相关文档

- 引脚简明表：[硬件引脚简明表.md](硬件引脚简明表.md)
- 引脚与阈值配置：`esp32-firmware/config.h`
- 软件部署（与本文档分开）：项目根目录 `README.md`、`WINDOWS.md`
- 无硬件联调：`demo/MQTTX测试指南.md`
